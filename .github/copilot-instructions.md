# Copilot instructions

This project's AI coding-agent instructions live in [`AGENTS.md`](../AGENTS.md)
at the repository root. Read that file for the actual guidelines.

(This is a plain file rather than a symlink like the other agent-instruction
aliases - some filesystems reject reading a symlink that crosses up into a
parent directory, which broke `nix develop` on at least one host.)
